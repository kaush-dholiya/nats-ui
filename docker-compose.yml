services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - nats-ui-data:/root/.nats-ui
    environment:
      - PORT=8080

  frontend:
    build: ./frontend-app
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  nats-ui-data:
